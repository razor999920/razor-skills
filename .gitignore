# Private — personal/work-specific material. Never publish.
private/

# Orphaned broken git dirs (sandbox could not delete their lock files).
# Any name starting with .git_broken — never track or publish these.
.git_broken*
.git_broken*/
